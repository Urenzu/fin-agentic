# The FastAPI service. Must be NEXT_PUBLIC_ because the canvas polls it from
# the browser -- ingestion takes seconds, so the client drives the poll loop
# rather than a server component holding a request open.
NEXT_PUBLIC_API_URL=http://localhost:8000
